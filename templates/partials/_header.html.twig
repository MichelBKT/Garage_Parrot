
<header>
  <nav class="navbar mb-5 navbar-expand-lg navbar-dark bg-dark text-white border-bottom sticky-top">
      <div class="container-fluid">
          <a href="/" class="navbar-brand">
            <img
              src=" {{ asset('build/images/logo rectangle.png') }} " 
              width="278" 
              height="80" 
              alt="logo" 
              class="mx-lg-2 img-fluid"
            >
          </a>
        <button 
          class="navbar-toggler" 
          type="button" 
          data-bs-toggle="collapse" 
          data-bs-target="#navbarNavDropdown" 
          aria-controls="navbarNavDropdown" 
          aria-expanded="false" 
          aria-label="Toggle navigation"
          >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link mx-lg-2 linkNav" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link mx-lg-2 linkNav" href="{{ path('app_used_car') }}">Véhicules d'occasion</a>
            </li>
            <li class="nav-item">
              <a class="nav-link mx-lg-2 linkNav" href="{{ path('app_maintenance_service')}}">Entretiens et services</a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle mx-lg-2 linkNav" href="#" role="button" data-bs-toggle="dropdown">Nous contacter</a>
              <ul class="dropdown-menu bg-dark">
                <li>
                  <a class="dropdown-item linkNav text-success" href="tel:0383838282">Par téléphone</a>
                </li>
                <li>
                  <a class="dropdown-item linkNav text-success" href="/#plan-toulouse">Nous localiser</a>
                </li>
                <li>
                  <a class="dropdown-item linkNav text-success" href="{{ path('app_form') }}">Nous écrire via un formulaire en ligne</a>
                </li>
              </ul>
            </li>
          </ul>
        {% if app.user %}
          <p class="text-success fw-bold mx-2 my-lg-3 linkNav">
            Bonjour {{app.user.firstName}} {{app.user.lastName|first}}.<br> {{app.user.workplace.designation}}
          </p>
          <div class="navbar-nav">
              <a href= "{% if is_granted('ROLE_ADMIN') %} {{ path('admin')}}
                        {% else %}
                          {{ path('app_user') }} 
                        {% endif %}"
                ><button type="button" class="btn btn-outline-secondary linkNav mx-lg-2 my-2">Mon compte</button>
              </a>
          </div>
        {% else %}
         <a href="{{ path('app_login') }}"><button type="button" class="btn btn-outline-success linkNav">Connexion</button></a> 
        {% endif %}
        </div>
      </div>
  </nav>

</header>
